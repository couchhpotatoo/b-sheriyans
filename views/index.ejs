<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
        <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body><div class="main w-full min-h-screen bg-zinc-900 text-white">
    <div class="form p-10">
        <form action="/create" method="post">
            <input type="text" class=" block w-full px-5 py-4 rounded-md outline-none bg-zinc-400" placeholder="input goes here" name="title" required>
            <textarea  class=" block px-5 w-full py-4 mt-4 rounded-md resize-none outline-none bg-zinc-700"placeholder="write your task details here" name="details"></textarea>
            <input class="bg-blue-500 mt-2 px-5 py-2 rounded-md" type="submit" value="create task"/>
        </form>
    </div>
    <div class="tasks flex gap-4 flex-wrap p-10 rounded-md">
        <% if(files.length > 0){ %>
        <% files.forEach(function(val){%>
<div class="task w-72   px-3 py-4 bg-zinc-800">
            <h1 class="text-white   text-3xl tracking-tighter"><%=val%></h1>
            <a href="#" class="text-blue-600 inline-block mt-3">read more</a>
        </div>
        <% })%>
        <%}else{%>
            <h3>no tasks yet</h3>
            <%}%>
        
        
        <div class="task w-72  hidden px-3 py-4 bg-zinc-800">
            <h1 class="text-white  text-3xl tracking-tighter">some task goes here</h1>
            <a href="#" class="text-blue-600 inline-block mt-3">read more</a>
        </div>
    
        <div class="task w-72 hidden  px-3 py-4 bg-zinc-800">
            <h1 class="text-white  text-3xl tracking-tighter">some task goes here</h1>
            <a href="#" class="text-blue-600 inline-block mt-3">read more</a>
        </div>
  
    </div>

    </div>
</body>
</html>